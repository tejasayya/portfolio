import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['github.com', 'images.unsplash.com', 'media.licdn.com', 'encrypted-tbn0.gstatic.com', 'alex-bezverkhniy.github.io', 'static-00.iconduck.com', 'raw.githubusercontent.com', 'cdn-lfs-us-1.hf.co', 'images.credly.com'],
  },
  /* config options here */
};

export default nextConfig;
